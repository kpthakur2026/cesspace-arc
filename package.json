{
  "name": "cesspace-arc",
  "version": "0.0.0-rc00",
  "description": "Secure, vendor-neutral agent-to-machine control plane for AI agents, development environments, and remote systems using MCP",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "check": "npm run check:docs && npm run check:secrets && npm run check:git",
    "check:docs": "bash scripts/check-docs.sh",
    "check:secrets": "bash scripts/check-secrets.sh",
    "check:git": "git diff --check",
    "verify:rc00": "bash scripts/verify-rc00.sh",
    "typecheck": "tsc --build",
    "test": "node --test tests/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
